## Usage

To install and run the application:

```
git clone git@github.com:diembzz/nux.git
cd nux
chmod +x ./build.sh && ./build.sh
docker-compose up
```

Wait for the nux-app container to load.
After that the following url will be available:

* http://localhost
